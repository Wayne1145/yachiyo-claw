# v0.0.20 candidate validation — 2026-09-12

Status: signed candidate, **not accepted for public release**.

Source: `b462d20` plus the local version changes in `package.json` and
`android/app/build.gradle` (0.0.20 / versionCode 20). The private keystore remains
under the ignored `.keys/` directory and is not part of the source tree.

## Signed artifact

- Application ID: `io.github.yachiyoclaw`
- APK size: 175444911 bytes
- APK SHA-256: `0894ce797ba1bddbba11d61983c6129a0cea7b06c916c90422a6f1e261247028`
- Certificate SHA-256: `60e81bcb2d72faf525d4dbfb952c956edbde60da93ac187b4f308df3bf4e9a23`
- APK Signature Scheme v2 verification passed.
- The certificate matches the official GitHub v0.0.19 `app-release.apk`.
- `verify:android-update-release` passed with previous version 0.0.19 / code 19.
- APK permission allowlist, telemetry endpoint scan and source-map/stats scan passed.

Local artifacts are saved in `.cache/releases/v0.0.20/` with a SHA-256 sidecar.

## Completed checks

- `assembleRelease`: passed in 19m30s using the repository wrapper with `-NoProxy`.
  The first attempt failed because Gradle tried an unavailable proxy at
  127.0.0.1:7890. The successful build retained D8 Kotlin metadata warnings.
- `testDebugUnitTest assembleDebug -x :app:syncAndroidWebAssets`: passed in 5m39s.
  Reused the unchanged renderer assets synchronized by the successful release build.
  App native tests: 38 suites, 123 tests, zero failures/errors.
- `pnpm check`: passed.
- Native log privacy check: passed for 117 source files.
- Flow Glass focused tests: 17 passed in the earlier run on the same renderer source.
- Initial full Vitest run: 266 files passed, 2 failed, 2 skipped; 2003 tests
  passed, 2 failed, 59 skipped. `migration.test.ts` timed out in its setup hook;
  `imageGenerationActions.test.ts` had two test timeouts. Focused single-worker
  rerun passed all 8 tests. This does not erase the initial full-run failures.
- Full rerun with `pnpm test --maxWorkers=2`: passed in 200.95s; 268 files
  passed, 2 skipped; 2010 tests passed, 54 skipped. No test timeout was increased.

## Device evidence

Android 15 / API 35 Google APIs x86_64, temporary AVD `Yachiyo_API35`, serial
`emulator-5554`. Physical display configuration: 1080x2400, density 420.

Removed the debug-certificate test app from this newly created test emulator,
installed the official signed v0.0.19, then installed the candidate with
`adb install --no-streaming -r`. Both installs succeeded. Version changed from
19 / 0.0.19 to 20 / 0.0.20 while `firstInstallTime` remained unchanged.
The candidate MainActivity was resumed and its process was running.

This is an ADB upgrade smoke test, not verification of the app's download,
unknown-source authorization, permission-return or system-installer UI flow.

## Outstanding acceptance

- Candidate startup logs still contain Chromium `tile memory limits exceeded`
  (12:28:50 and 12:28:54 device time). The earlier CSS changes have not proved
  elimination of this issue.
- Earlier rotation commands and a running Activity do not establish visual
  correctness in landscape. Complete visual/layout checks are still required.
- A previous 1440x3200 override coincided with ADB going offline. Its cause was
  not established; it must not be reported as confirmed GPU exhaustion.
- Android 11/13 device matrix and physical-device regression remain pending.
- Real API Tetris Agent workflow, interruption/output handling, Ubuntu/Alpine
  separation and default-browser delivery remain pending.
- Full in-app update download/authorization/installer regression remains pending.

Do not mark these items passed based on static CSS tests, APK existence or
the absence of errors in a short tail of Logcat. Publish only after the requested
acceptance has been completed.
